<%= form_for @schedule do |f| %>


    <%= f.label :maid_id, "Request Maid"%>
    <%= f.collection_select :maid_id, @maids, :id, :first_name %><br><br>

    <%= f.submit "Submit Request" %>
    <% end %>